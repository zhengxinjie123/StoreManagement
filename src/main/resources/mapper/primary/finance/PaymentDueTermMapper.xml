<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.joao.storemanagement.mapper.primary.finance.PaymentDueTermMapper">

    <resultMap id="PaymentDueTermMap" type="com.joao.storemanagement.entity.finance.PaymentDueTerm">
        <id property="id" column="id"/>
        <result property="code" column="code"/>
        <result property="label" column="label"/>
        <result property="offsetDays" column="offset_days"/>
        <result property="offsetMonths" column="offset_months"/>
        <result property="unknownTerm" column="unknown_term"/>
        <result property="sortOrder" column="sort_order"/>
        <result property="active" column="active"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="selectActiveOrdered" resultMap="PaymentDueTermMap">
        SELECT id, code, label, offset_days, offset_months, unknown_term, sort_order, active, created_at, updated_at
        FROM payment_due_terms
        WHERE active = 1
        ORDER BY sort_order ASC, id ASC
    </select>

    <select id="selectAllOrdered" resultMap="PaymentDueTermMap">
        SELECT id, code, label, offset_days, offset_months, unknown_term, sort_order, active, created_at, updated_at
        FROM payment_due_terms
        ORDER BY sort_order ASC, id ASC
    </select>

    <select id="selectByCode" resultMap="PaymentDueTermMap">
        SELECT id, code, label, offset_days, offset_months, unknown_term, sort_order, active, created_at, updated_at
        FROM payment_due_terms
        WHERE code = #{code} AND active = 1
    </select>

    <select id="selectById" resultMap="PaymentDueTermMap">
        SELECT id, code, label, offset_days, offset_months, unknown_term, sort_order, active, created_at, updated_at
        FROM payment_due_terms
        WHERE id = #{id}
    </select>

    <update id="update">
        UPDATE payment_due_terms
        SET label = #{label},
            offset_days = #{offsetDays},
            offset_months = #{offsetMonths},
            unknown_term = #{unknownTerm},
            sort_order = #{sortOrder},
            active = #{active},
            updated_at = SYSUTCDATETIME()
        WHERE id = #{id}
    </update>
</mapper>

