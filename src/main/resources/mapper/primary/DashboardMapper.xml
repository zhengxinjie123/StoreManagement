<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.joao.storemanagement.mapper.primary.DashboardMapper">

    <select id="countTodayUploadedInvoices" resultType="long">
        SELECT COUNT(1)
        FROM import_attachments
        WHERE upload_date &gt;= CAST(GETDATE() AS DATE)
          AND upload_date &lt; DATEADD(day, 1, CAST(GETDATE() AS DATE))
    </select>

    <select id="countPendingCleanInvoices" resultType="long">
        SELECT COUNT(1)
        FROM import_attachments
        WHERE clean_status = 0
    </select>

    <select id="countImportFailedInvoices" resultType="long">
        SELECT COUNT(1)
        FROM import_attachments
        WHERE import_status = 0
    </select>

    <select id="sumPendingPaymentAmount" resultType="java.math.BigDecimal">
        SELECT ISNULL(SUM(amount), 0)
        FROM supplier_payment_pending
        WHERE status = N'open'
    </select>

    <select id="countOverduePayments" resultType="long">
        SELECT COUNT(1)
        FROM supplier_payment_pending
        WHERE status = N'open'
          AND payment_due_date IS NOT NULL
          AND payment_due_date &lt; CAST(GETDATE() AS DATE)
    </select>

    <select id="countPendingReplenishRecords" resultType="long">
        SELECT COUNT(1)
        FROM replenish_records
        WHERE status = N'pending'
    </select>
</mapper>
